# Test case coverage audit

This document is the canonical index for Harmonius test-case coverage across design artifacts.

## Scope

Coverage spans `TC-*` rows in companion files such as `docs/design/integration/*-test-cases.md` and
subsystem-specific test case supplements.

## Status

The full audit tables are not published yet. Until they land here, treat companion test-case files
next to each integration or subsystem design as the authoritative `TC-*` inventory for that slice.
