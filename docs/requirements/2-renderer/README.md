# Renderer Requirements

These requirements do not apply to this framework. These requirements would
apply to a renderer built using this framework. They help us understand what
features renderers would actually consider building, and what requirements would
apply to users building renderers with the framework.
