# Geometry and World Requirements

Requirements for geometry and world systems are currently tracked within the rendering domain under
the renderer requirements (R-2.7.x for geometry/meshes, R-2.8.x for worlds/terrain, R-2.16.x for
foliage/vegetation). See [rendering/renderer.md](../rendering/renderer.md) for the full listing.
