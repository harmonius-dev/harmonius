# 1.1 Core Constraints

## 1.1.1 Safe User-Facing API

The user-facing API SHALL be 100% safe Rust. Unsafe code SHALL be confined to background render and IO threads behind a strict FFI boundary. This guarantees that user code cannot trigger undefined behavior.

## 1.1.2 GPU-Driven Rendering

All rendering SHALL be GPU-driven. Draw calls SHALL be dispatched by the GPU via indirect dispatch buffers populated by compute culling passes. No CPU-side draw call submission SHALL exist. This eliminates CPU draw-call bottlenecks and enables massive scene complexity.

## 1.1.3 Mesh Shader Pipeline

Mesh shaders (task/object + mesh stages) SHALL be the sole geometry pipeline. No traditional vertex/geometry/tessellation pipeline SHALL be supported. This reduces code surface and enables meshlet-level GPU culling.

## 1.1.4 Declarative Render Graph

The rendering pipeline SHALL be defined as a declarative, frame-invariant directed acyclic graph of passes and resources. The graph SHALL be compiled once into an optimized execution plan and re-executed each frame with only per-frame data changing. This enables automatic barrier insertion, resource aliasing, and pass scheduling.

## 1.1.5 Cross-Platform via Native Backends

Each GPU API SHALL be supported through a native backend. No compatibility/translation layers (MoltenVK, DXVK, etc.) SHALL be used. Direct native API access ensures maximum performance and avoids translation overhead.

## 1.1.6 Modern Hardware Only

The system SHALL require bindless resources, mesh shaders, and hardware ray tracing (for RT features). No legacy fallback paths SHALL exist. This keeps the codebase lean and avoids maintaining parallel code paths for deprecated hardware.

## 1.1.7 Excluded Scope

The following SHALL be out of scope:
- Legacy pipelines: geometry shaders, tessellation, traditional vertex pipeline
- Console and mobile platforms: Xbox, PlayStation, Switch, iOS, Android
- CPU-driven rendering of any kind
- Compatibility/translation layers between GPU APIs
