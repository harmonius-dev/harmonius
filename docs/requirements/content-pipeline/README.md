# Content Pipeline Requirements

Shader, asset, and streaming/IO requirements are currently tracked within the rendering domain under
R-2.11.x (shader and assets) and R-2.12.x (streaming and IO). See
[rendering/renderer.md](../rendering/renderer.md) for the full listing.
