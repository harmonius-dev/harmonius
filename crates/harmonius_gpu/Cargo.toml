[package]
description = "GPU backend traits and CPU-side render pipeline helpers (allocators, state, barriers)."
edition = "2021"
license = "Apache-2.0"
name = "harmonius_gpu"
publish = false
rust-version = "1.80"
version = "0.1.0"

[dependencies]
