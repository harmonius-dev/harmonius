[package]
description = "Input–camera integration (IR-4.1.*) per docs/design/integration/input-camera.md."
edition = "2021"
license = "MIT OR Apache-2.0"
name = "integration_input_camera"
version = "0.1.0"

[dependencies]
crossbeam-channel = "0.5"
glam = "0.29"
tracing = "0.1"

[dev-dependencies]
tracing-subscriber = { version = "0.3", default-features = false, features = [
  "registry",
  "std",
] }
