[package]
description = "No-op console SDK stub for desktop and editor builds"
edition = "2021"
license = "Apache-2.0"
name = "console_stub"
version = "0.1.0"

[lib]
path = "src/lib.rs"

[dependencies]
console_sdk = { path = "../console_sdk" }
