[package]
description = "Deterministic render-effects helpers (post, lighting, RT) for Harmonius TC coverage"
edition = "2021"
license = "Apache-2.0"
name = "render-effects"
rust-version = "1.80"
version = "0.1.0"

[lib]
path = "src/lib.rs"
