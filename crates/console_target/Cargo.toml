[package]
description = "Build-time console platform detection from rustc target triples"
edition = "2021"
license = "MIT OR Apache-2.0"
name = "console_target"
version = "0.1.0"

[lib]
path = "src/lib.rs"
