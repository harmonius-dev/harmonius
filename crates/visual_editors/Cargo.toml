[package]
authors = ["Harmonius Contributors"]
description = "Visual editor graph IR, validation, and compilation helpers"
edition = "2021"
license = "Apache-2.0"
name = "visual_editors"
version = "0.1.0"

[dependencies]
glam = { version = "0.29", default-features = false, features = ["libm"] }
smallvec = "1.14"
uuid = { version = "1.11", features = ["v4"] }
