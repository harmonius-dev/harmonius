[package]
license = "Apache-2.0"
edition = "2021"
name = "harmonius_integration_rendering_geometry"
rust-version = "1.85"
version = "0.1.0"

[dependencies]
glam = { version = "0.29", default-features = false, features = ["libm"] }
