[package]
description = "Harmonius core crate root: console backend selection and shared cfg"
edition = "2021"
license = "MIT OR Apache-2.0"
name = "harmonius_core"
version = "0.1.0"

[lib]
path = "src/lib.rs"

[dependencies]
console_sdk = { path = "../console_sdk" }
console_stub = { path = "../console_stub" }

[build-dependencies]
console_target = { path = "../console_target" }
