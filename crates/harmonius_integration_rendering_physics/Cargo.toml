[package]
authors = ["Harmonius"]
description = "Rendering–physics integration contracts (debug draw, interpolation alpha)."
edition = "2021"
license = "MIT OR Apache-2.0"
name = "harmonius_integration_rendering_physics"
repository = "https://github.com/cjhowe-us/harmonius"
rust-version = "1.85"
version = "0.1.0"

[dependencies]
glam = { version = "0.29.2", default-features = false, features = ["std"] }
