[package]
authors = ["Harmonius Contributors"]
description = "Deterministic math and render-queue helpers for the Harmonius rendering core."
edition = "2021"
license = "MIT OR Apache-2.0"
name = "harmonius_rendering_core"
repository = "https://github.com/cjhowe-us/harmonius"
rust-version = "1.74"
version = "0.1.0"

[dependencies]
glam = { version = "0.29.2", default-features = false, features = ["std"] }
