[package]
authors = ["Harmonius contributors"]
description = "Physics ↔ world geometry integration contracts (colliders, layers, heightfields)."
edition = "2021"
license = "Apache-2.0"
name = "integration_physics_geometry"
repository = "https://github.com/cjhowe-us/harmonius"
rust-version = "1.85"
version = "0.1.0"

[dependencies]
glam = { version = "0.29", default-features = false, features = ["std"] }
