[package]
edition = "2021"
name = "harmonius_integration_rendering_camera"
rust-version = "1.85"
version = "0.1.0"

[dependencies]
crossbeam-channel = "0.5"
glam = { version = "0.29", default-features = false, features = ["libm"] }
log = "0.4"

[[bench]]
harness = false
name = "snapshot_build"

[dev-dependencies]
criterion = "0.5"
