[package]
description = "Wire types and SaveRpc bridge for networking ↔ save integration (IR-4.6)."
edition = "2021"
license = "Apache-2.0"
name = "integration_networking_save"
publish = false
version = "0.1.0"

[dependencies]
crossbeam-channel = "0.5.15"
rkyv = { version = "0.7.46", features = ["validation"] }
