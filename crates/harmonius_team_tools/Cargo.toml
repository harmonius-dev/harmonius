[package]
license = "Apache-2.0"
edition = "2021"
name = "harmonius_team_tools"
version = "0.0.0"

[lib]
path = "src/lib.rs"

[dependencies]
git2 = { default-features = false, features = ["vendored-libgit2"], version = "0.20" }

[dev-dependencies]
tempfile = "3"
