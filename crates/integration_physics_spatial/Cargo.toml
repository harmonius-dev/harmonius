[package]
authors = ["Harmonius Contributors"]
description = "Physics-private BVH integration contracts (physics ↔ spatial index bridge)."
edition = "2024"
license = "MIT OR Apache-2.0"
name = "integration_physics_spatial"
readme = "README.md"
repository = "https://github.com/cjhowe/harmonius"
rust-version = "1.85"
version = "0.1.0"

[dependencies]

[dev-dependencies]
trybuild = "1.0.103"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "deny"
