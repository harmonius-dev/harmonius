[package]
edition = "2021"
name = "networking_audio_integration"
rust-version = "1.85"
version = "0.1.0"

[dependencies]
crossbeam-channel = "0.5"
hmac = "0.12"
opus-rs = "0.1.12"
rkyv = { version = "0.8.15", default-features = false, features = ["bytecheck", "std"] }
sha2 = "0.10"

[dev-dependencies]
crossbeam-channel = "0.5"
