[package]
authors = ["Harmonius Contributors"]
description = "Rendering–UI integration contracts (IR-3.6.*, TC-IR-3.6.*)."
edition = "2021"
license = "MIT OR Apache-2.0"
name = "rendering_ui"
version = "0.1.0"

[dependencies]
glam = { default-features = false, features = ["std"], version = "0.29" }
