[package]
edition = "2021"
license = "Apache-2.0"
name = "harmonius-integration-tests"
version = "0.0.1"

[dependencies]
blake3 = "1.8"
harmonius_asset_build = { path = "../harmonius_asset_build" }
rkyv = { version = "0.8.9", features = ["std"] }

[dev-dependencies]
