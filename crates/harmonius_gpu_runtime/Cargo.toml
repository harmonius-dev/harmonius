[package]
description = "GPU profiler integration types shared by rendering and tools"
edition = "2021"
license = "Apache-2.0"
name = "harmonius_gpu_runtime"
publish = false
version = "0.1.0"

[dependencies]
crossbeam-channel = "0.5"
tracing = "0.1"
