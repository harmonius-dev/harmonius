[package]
description = "Audio–physics integration bridge (impact, friction, trigger zones)"
edition = "2021"
license = "MIT OR Apache-2.0"
name = "harmonius_integration_audio_physics"
readme = "README.md"
version = "0.1.0"

[dependencies]

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "deny"
