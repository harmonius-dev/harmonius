[package]
name = "harmonius_net"
version = "0.1.0"
edition = "2021"
description = "Harmonius network transport, replication, prediction, and RPC (sans-io building blocks)."
license = "Apache-2.0"

[dependencies]
aes-gcm = "0.10"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "deny"
