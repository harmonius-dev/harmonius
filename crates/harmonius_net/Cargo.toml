[package]
name = "harmonius_net"
version = "0.1.0"
edition = "2021"
description = "Harmonius networking: transport, replication, prediction, RPC, session services, replay, and communication."
license = "Apache-2.0"

[dependencies]
aes-gcm = "0.10"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "deny"
