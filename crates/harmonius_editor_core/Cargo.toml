[package]
edition = "2021"
name = "harmonius_editor_core"
rust-version = "1.85"
version = "0.1.0"

[dependencies]
glam = { version = "0.29", default-features = false, features = ["libm"] }
smallvec = { version = "1.15", features = ["const_generics"] }
