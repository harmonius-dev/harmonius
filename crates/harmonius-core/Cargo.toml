[package]
description = "Harmonius core runtime: typed events and plugin composition"
edition = "2021"
license = "Apache-2.0"
name = "harmonius-core"
version = "0.1.0"

[features]
default = []
hot-reload = []

[dependencies]
