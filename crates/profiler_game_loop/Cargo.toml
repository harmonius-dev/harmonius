[package]
description = "Profiler integration with the game loop frame pipeline"
edition = "2021"
license = "MIT OR Apache-2.0"
name = "profiler_game_loop"
version = "0.1.0"

[lib]
path = "src/lib.rs"
