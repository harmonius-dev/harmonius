[package]
authors = ["Harmonius Contributors"]
description = "Timelines ↔ camera integration contracts and binding logic."
edition = "2021"
license = "MIT OR Apache-2.0"
name = "integration_timelines_camera"
repository = "https://github.com/cjhowe-us/harmonius"
rust-version = "1.85"
version = "0.1.0"

[dependencies]
glam = "0.29"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = { level = "deny", priority = -1 }
